# PAErealtimeImaging 项目接管说明（HANDOFF）

更新时间：2026-09-16  
面向对象：本地 Codex 新会话  
仓库：`ZiXuannnZhang/PAErealtimeImaging`

---

# 1. 当前状态

当前主工作分支：

```text
codex/physical-round-normalizer-integrated-20260916
```

当前已确认远端 HEAD：

```text
52cf7713d7e0e935cb14663ec3470f3a25bfeb90
```

本轮 RoundIdentity / Final Marker 真正源码+测试提交：

```text
26d54b5a2b564a127b046ab213a7f6d09b8c83b9
```

后续报告提交：

```text
d721f4c540a2a5ac0b5c1d97a90a38e6e6013e65
52cf7713d7e0e935cb14663ec3470f3a25bfeb90
```

注意：`52cf771...` 只是回填报告 SHA 的文档提交，不包含新的生产源码改动。

当前软件验证状态：

```text
AUTOMATED_TESTS_PASS
WINDOWS_BUILD_PASS
CUDA_SERVICE_SELFTEST_PASS
HARDWARE_VALIDATION_PENDING
```

**最重要：物理轮次归一（PhysicalRoundNormalizer / RoundIdentity）尚未完成实机验收，不得标记为“已完成”或“已验收”。**

软件层三个已知生产阻塞点已经整改并通过自动化/服务级自检，但最新实机环境出现了新的事实：

```text
旧控制环境：约 4001 physical triggers / round
新控制环境：约 4007 physical triggers / round
```

因此此前 `4001 physical -> 1 operational/control + 4000 logical` 的实机依据已经不足。下一步第一优先级不是继续改代码，而是本地解析最新实机日志，重新建立真实物理轮行为证据。

---

# 2. 当前最重要的未完成工作

## P0：确认完整物理轮是否稳定为 4007

最新实机诊断包较大，网页环境无法可靠解压。由本地 Codex 直接解析。

必须回答：

```text
1. 每个完整物理轮的 distinct physical trigger 数是否恒定为 4007？
2. 是否存在 4006 / 4008 / 其他非 4007 的完整轮？
3. triggerSeq 是否连续，是否有 gap / duplicate / wrap？
4. 是否有 partial/incomplete frame identity？
```

用户明确说明：触发数明显过少的轮次属于手动暂停，不纳入“完整轮触发数恒定性”统计。

但不要仅因为 `<4007` 就自动归类为手动暂停。应尽量结合：

```text
stop/pause 时间
idle timeout
session 状态
boundary 事件
trigger 时间间隔
```

证明短轮确实与人工暂停一致。证据不足时必须写：

```text
无法判断该短轮是否由手动暂停导致
```

不得自行补结论。

---

## P0：验证 CountBoundary / 圈末重置

重点搜索：

```text
count_boundary_reset
CountBoundary
countBoundaryResets
round_generation
measurement_session
currentLogicalDistinctCount
logicalDistinctAccepted
physicalDistinctObserved
round_control_filtered
RoundIdentity
source_round_complete
reconstruction_complete
round_block_count_mismatch
```

必须回答：

```text
A. CountBoundary 实际在哪个 trigger 上发生？
B. 每个完整 4007-trigger 物理轮内发生几次 CountBoundary？
C. CountBoundary 后 roundGeneration 如何变化？
D. 下一可见 trigger 是否被作为 OperationalStartupControl？
E. CountBoundary 是否与真实物理轮边界重合？
F. 是否存在真实轮未结束但 Normalizer 已提前 CountBoundary？
G. 是否存在真实轮结束但没有 CountBoundary？
```

如果日志没有足够证据，直接结论：

```text
无法从当前日志证明 CountBoundary / 圈末重置运行正常
```

不要因为代码和自动化测试通过就推断实机正常。

---

## P0：验证 TimeoutBoundary / 超时重置完整链

重点搜索：

```text
timeout_boundary_reset
TimeoutBoundary
timeoutBoundaryResets
physical-idle-timeout
ring_reset
epoch_reset
submit_index
stale frame dropped
round identity stale drop
```

期望证据链：

```text
partial physical round
→ 足够长 idle
→ PhysicalRoundNormalizer TimeoutBoundary
→ roundGeneration 前进
→ RingBlockAssembler residual/reset
→ Controller/MainWindow 发 ring_reset
→ ImagingSvc epoch_reset / close + resetRingRecon
→ timeout 前旧 submit_index / RoundIdentity 后续 stale-drop
→ 下一 RoundIdentity clean start
```

如果只看见其中部分，例如有 `timeout_boundary_reset`，但没有下游 `ring_reset / epoch_reset / stale-drop / next clean start`，只能结论：

```text
TimeoutBoundary 被记录，但无法证明完整超时重置链正常
```

无证据时必须明确写“无法判断”。

---

# 3. 关于 4007 的关键处理原则

**禁止因为新环境观察到 4007，就直接把配置从 4000 改成 4006。**

也不能直接把：

```text
4001 physical -> 1 control + 4000 logical
```

机械替换成：

```text
4007 physical -> 1 control + 4006 logical
```

目前不知道新增 6 个 trigger 的真实语义。可能来自：

```text
控制器新增 pre/post triggers
额外 operational/control triggers
重复触发
状态切换边界 trigger
真实 logical scan 数变化
不同控制环境 trigger 序列变化
日志统计口径变化
```

这些在有日志证据前都只是可能性。

正确顺序：

```text
先重建 distinct triggerSeq / 时间序列
→ 对齐每张卡
→ Normalizer decision
→ logicalTriggerIndex
→ roundGeneration
→ CountBoundary / TimeoutBoundary
→ Ring source_round_complete
→ 再判断旧 first-visible operational 模型是否成立
```

如模型不成立，再单独设计下一轮源码整改。

---

# 4. 当前 RoundIdentity / Ring 架构背景

RoundIdentity：

```text
(measurementSession, roundGeneration)
```

其中：

```text
measurementSession != 0 才 valid
roundGeneration = 0 合法
```

数据链：

```text
PhysicalRoundNormalizer
→ TriggerGroup
→ RingBlockAssembler
→ ImagingController
→ ImagingSvc
→ ring_snapshot_ready
→ MainWindow
```

RingBlockAssembler hard barrier 当前行为：

```text
同 identity：正常累计
新 identity：丢旧 residual/phase 后接新数据
旧 identity：stale drop
禁止 block 混合不同 identity
```

除非新硬件日志直接证明 barrier 有问题，不要重新设计。

---

# 5. 已整改的软件阻塞点（背景，不代表硬件验收完成）

## Blocker 1：ImagingSvc reconstruction 跨轮污染

ImagingSvc 已有 active RoundIdentity barrier。新 identity 会 `resetRingRecon()`，清：

```text
CUDA accumulators
m_ringBlockIndex
m_ringPrevWL2
angle/radius state
```

旧 identity stale-drop。

CUDA identity-jump selftest 已验证新轮首图相对 clean first image：

```text
max_pixel_difference = 0
```

## Blocker 2：snapshot metadata 与 SHM pixels 错配

Controller 现在要求：

```text
notification seq == SHM frame_seq
```

并在同一 SHM lock 内完成 exact seq check + pixel copy。不匹配 fail closed，并释放 buffer。

## Blocker 3：source end != reconstruction complete

当前语义：

```text
sourceRoundComplete
= 上游观察到最后 logical trigger
= 只决定 reconstruction lifecycle close/reset

reconstructionComplete
= sourceRoundComplete && blocksConsumed == expectedBlocks
= 唯一允许 final PNG / presentation completion 的事实
```

Host→Svc：

```text
ring_block_ready.source_round_complete
```

Svc→Host：

```text
ring_snapshot_ready.round_complete = reconstructionComplete
```

中间 block 丢失但 final source block 仍到达时：

```text
round_complete=false
round_block_count_mismatch
close/reset old round
next round clean start
```

真实 ImagingSvc + CUDA `--drop-block 2` selftest 已通过。

---

# 6. Final Marker 软件整改

此前 one-shot `roundComplete` 可能随 Ring-disabled card 被消耗。

现在拆分：

```text
roundComplete = one-shot classification/control pulse
isFinalLogicalTrigger = stable per-trigger data property
```

最后 logical trigger：

```text
first classification:
  roundComplete=true
  isFinalLogicalTrigger=true

cached/late card:
  roundComplete=false
  isFinalLogicalTrigger=true
```

Ring 数据面使用 `isFinalLogicalTrigger`。

CountBoundary observer 仍 one-shot。

自动化已覆盖 `disabled card first / enabled card later`。

**这只是软件测试通过，不代表 PhysicalRoundNormalizer 已完成硬件验收。**

---

# 7. 当前必须保持的验收状态

```text
HARDWARE_VALIDATION_PENDING
```

尚未实机确认：

```text
真实完整物理轮是否稳定为 4007
4007 个 trigger 的语义组成
first-visible operational filter 在新环境是否仍成立
configured logical trigger count 是否正确
CountBoundary 是否真实对应圈末
TimeoutBoundary 是否完整驱动 Ring/UI/ImagingSvc reset
多轮连续采集是否无跨轮污染
最终 PNG / presentation 是否匹配正确 RoundIdentity
```

因此禁止写：

```text
PhysicalRoundNormalizer completed
RoundIdentity hardware validated
4001→4000 contract confirmed
```

---

# 8. 已踩过的关键坑

## 坑 1：把 host operational classification 当成 FPGA 协议字段

`OperationalStartupControl` 是 host 侧 operational classification。Wire/FPGA 目前只有 triggerSeq 证据，不要声称硬件存在明确 control-trigger type。

## 坑 2：Normalizer count != Ring consumed block count

Normalizer 能观察 partial/incomplete CardFrame identity；Ring/HostOutput 只消费完整同步数据。

因此必须保留：

```text
RoundIdentity barrier
sourceRoundComplete / reconstructionComplete 分离
```

## 坑 3：CountBoundary 不能立即 reset Ring/ImagingSvc

CountBoundary 可能发生在最后 logical trigger 第一张 card 到达时，其他 card 尚未 fan-in。立即 reset 会清掉最后 trigger 的残余数据。

当前原则：

```text
CountBoundary -> control/presentation/save binding
不直接 reset data plane
```

## 坑 4：UI delivered snapshot 数不能代表 producer 完整度

Display/SHM 是 latest-wins/double-buffer，UI 可能丢中间 snapshot。

禁止用：

```text
deliveredSnapshots % blocksPerFrame
```

推断 final。

## 坑 5：snapshot seq 与 pixels 必须原子绑定

不能收到 seq=N 后稍晚再读当前 SHM pixels；可能已经变成 N+1。当前 exact seq/same-lock 绑定不可回退。

## 坑 6：报告记录自己的最终 HEAD 会产生自引用

曾经为了回填 `REMOTE_HEAD_SHA` 又生成新 commit，导致报告再次过时。

以后最终 remote HEAD 放在 Codex 回执里，不要反复回填报告。

## 坑 7：独立 copied repo 导致无共同祖先

曾有独立历史：

```text
codex/physical-round-blockers-fix-20260916
```

后来已 cherry-pick 到正常 integrated 历史。后续不要重新用独立 Git 历史做主工作。

## 坑 8：Windows 深路径导致测试构建假失败

CMake 对象目录 + worktree 深路径可能超过 Win32 260 字符，表现为 depfile 创建失败。

解决方式是缩短 tests build tree，例如 worktree 根 `/build/tests`，不要因此修改生产源码。

---

# 9. 当前不应主动重开的冻结区

除非最新硬件日志直接指向这些模块，否则暂时不要重开：

```text
DataProcessor batch-boundary dequeue
network/source observability
PREPARING/ARMED/RUNNING lifecycle
START admission fence
diagnostics export
Ring SHM observability core
shared multicard classification core
SourceCore packet-assembly timeout isolation
FileSaver roundGeneration rollover
AutoSaveRoundCoordinator mapping core
snapshot submit_index cutoff
RingReconRoundState barrier
RingSnapshotCopy exact seq binding
CUDA numerical algorithm
FPGA protocol
binary save format
SocketReceiver/UDP core
```

先证明问题来自真实控制环境 trigger pattern，还是软件 bug。

---

# 10. 本地 Codex 下一步执行流程

## Step 1：锁定代码基线

```bash
git fetch origin
git checkout codex/physical-round-normalizer-integrated-20260916
git status --short
git rev-parse HEAD
```

预期 HEAD：

```text
52cf7713d7e0e935cb14663ec3470f3a25bfeb90
```

若远端已继续前进，记录新 SHA，不要自动 reset/rebase。

## Step 2：找到并解压最新实机诊断包

日志时间约：

```text
2026-09-16 21:17
```

文件名类似：

```text
diagnostic-43842e97-5d47-4d6c-a996-6e7f7797654f-20260916-211718 (2).zip
```

解压到独立分析目录，不修改原始日志。

## Step 3：先做日志清单

记录所有：

```text
json/jsonl/csv/txt/trace/summary/diagnostic/network/round/event
```

并说明时间范围、条数、关键字段、是否有 session/generation/triggerSeq。

## Step 4：生成物理轮统计表

建议：

```text
analysis/physical_round_trigger_counts_20260916.csv
```

至少包含：

```text
measurement_session
candidate_round_index
start_time
end_time
duration
first_trigger_seq
last_trigger_seq
distinct_trigger_count
duplicate_count
gap_count
partial_or_incomplete_count
count_boundary_count
timeout_boundary_count
manual_pause_candidate
evidence
```

核心只统计 distinct physical trigger identity，不把不同 card 的同一 trigger 重复计数。

## Step 5：区分完整轮与短轮

对可确认完整轮输出：

```text
N full rounds
count=4007: X
count!=4007: Y
min
max
median
distribution
```

短轮单独列出，并注明手动暂停证据。

## Step 6：重建 CountBoundary 时间线

每个完整轮至少列：

```text
real round start
trigger #1 ... #4007
CountBoundary timestamp/trigger
roundGeneration before/after
next OperationalStartupControl
```

目标：判断 CountBoundary 是否与真实轮末一致。

## Step 7：重建 TimeoutBoundary 时间线

每个短轮/暂停点列：

```text
last physical trigger
idle duration
timeout threshold
TimeoutBoundary
ring_reset
epoch_reset
old RoundIdentity stale drops
new generation first trigger
new Ring block/snapshot
```

证据不完整时明确写“只能证明到哪一步”。

## Step 8：先提交分析报告，不改生产代码

优先生成：

```text
CODEX_REPORTS/hardware-round-trigger-analysis-20260916.md
```

在事实未确定前：

```text
不要修改 configuredLogicalTriggersPerRound
不要修改 first-visible filtering
不要修改 CountBoundary 语义
不要修改 FPGA/UDP
```

如分析证明当前归一模型与新环境不兼容，再单独设计源码整改任务。

---

# 11. 推荐日志分析报告结构

```text
# 1. 数据来源
日志文件
时间范围
软件 SHA

# 2. 完整物理轮 trigger count
逐轮表
排除的手动暂停短轮
4007 是否恒定

# 3. CountBoundary 证据
逐次事件
对应 trigger
generation 变化
是否与真实圈末重合

# 4. TimeoutBoundary 证据
partial round
idle
timeout event
ring_reset
epoch_reset
stale drop
new round

# 5. 无法判断项
明确列出日志缺失的证据

# 6. 结论
SUPPORTED / NOT_SUPPORTED / INCONCLUSIVE

# 7. 是否建议改代码
只基于证据提出，不直接实施
```

---

# 12. 当前推荐状态标签

```text
ROUND_IDENTITY_CODE_FIXES = IMPLEMENTED
ROUND_IDENTITY_AUTOMATED_TESTS = PASS
ROUND_IDENTITY_WINDOWS_BUILD = PASS
ROUND_IDENTITY_CUDA_SERVICE_SELFTEST = PASS

PHYSICAL_ROUND_NORMALIZATION_HARDWARE_ACCEPTANCE = PENDING
COUNT_BOUNDARY_HARDWARE_BEHAVIOR = UNVERIFIED_IN_NEW_4007_ENVIRONMENT
TIMEOUT_BOUNDARY_HARDWARE_BEHAVIOR = UNVERIFIED_IN_NEW_4007_ENVIRONMENT
FULL_ROUND_TRIGGER_COUNT_4007 = TO_BE_VERIFIED_FROM_LOCAL_LOGS
```

不要将上述 `PENDING / UNVERIFIED` 项改成 PASS，除非本地日志或后续实机测试提供直接证据。

---

# 13. 一句话接管目标

**先用本地日志证明新控制环境下真实物理轮如何形成——重点确认 4007 是否稳定，以及 CountBoundary / TimeoutBoundary 是否与真实边界一致；再决定 PhysicalRoundNormalizer 是否需要下一轮整改。在此之前，禁止把当前物理轮次归一工作标记为完成。**
