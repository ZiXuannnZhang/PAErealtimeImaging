# Historical HANDOFF — realtime_imaging_migration (2026-09-05/06)

> **HISTORICAL / SUPERSEDED**
>
> This document preserves the role of the former root `HANDOFF.md` from the migration phase. It is retained only for traceability.
>
> Do **not** use it for current branch selection, build commands, delivery layout, local workspace cleanup, or current project status.
>
> Current authoritative entry points are:
>
> 1. `PROJECT_STATUS.md`
> 2. `REPOSITORY_BASELINE.md`
> 3. `BUILD_STANDARD.md`
> 4. `Codex-GitHub双端联动快速上手.md`
> 5. the current task document under `codex/task-docs:TASKS/`
>
> The original migration handoff contained machine-specific paths, old remotes, old build directories such as `build/mingw_make`, local-uncommitted-work descriptions, and sandbox/migration notes from 2026-09-05/06. Those details are intentionally not promoted into the current operating documentation. The complete original text remains recoverable from Git history at `main` commit `1a49c2f1eb2d9769be6829cdd6e36dc507f93ad5` and earlier.

## Historical scope

The superseded handoff documented the repository migration from an older local workspace, early Ring imaging integration, per-channel sysDelay experiments, 250 MHz sampling normalization, legacy MinGW Makefiles build paths, CUDA rebuild notes, RadiusCalibration work, and local sandbox/workspace conditions.

Some technical history remains useful for archaeology, but none of the following should be assumed current without checking the current source and project-level documents:

- old absolute paths such as `D:\DSHWorkspace\realtime_imaging_migration`;
- the old local bundle remote;
- `build/mingw_make` as the canonical build directory;
- statements about uncommitted local changes;
- old executable naming such as `MC410T_Receiver.exe`;
- old sandbox-specific restrictions;
- statements that the historical handoff is the "new-session sole entry point".

For current work, begin with `PROJECT_STATUS.md` instead.
