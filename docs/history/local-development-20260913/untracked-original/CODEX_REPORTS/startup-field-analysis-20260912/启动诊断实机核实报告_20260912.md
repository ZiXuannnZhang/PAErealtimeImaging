# 启动诊断构建实机分析报告

证据附件：diagnostic-51e43a33-6eb6-4a69-8e4a-e8430d58e0e5-20260912-000501.zip。分析的实机时间为2026-09-11 23:56至2026-09-12 00:04（UTC+8）。

## 结论

启动段数据缺失仍存在，且发生在应用成功recvfrom原始入口之前。本次原启动缓存／过滤已实际旁路，没有源过滤、组包主动清理或输出队列丢弃记录可以解释这些缺失。

本附件覆盖的四轮中，三轮启动时有整触发号缺口，分别4、36、4个；另一轮4001个触发均完整。进入随后连续段后，四卡没有整触发跳号或部分缺包。因此不能确认用户所说的“采集中始终存在完整有效触发缺失”为独立的运行中持续故障。本次完整记录数不足可由启动段缺失解释。

根因尚不能定位到接收侧硬件、驱动、系统网络层或应用接收调度中的具体一层。没有找到本次系统抓取ETL／PcapNG／成功manifest，不得据此认定硬件故障，也不应继续猜测性重构。

## 代码和构建身份

检查独立worktree D:/ChatGPT/PAERealtimeImaging/_startup_diagnostics_20260911，工作区干净，最新提交9d0fd7a为交付文档；实际实现提交865a686f1b24f64e0c0d31125c23b7dc61dbc40a。

用户指定EXE当前SHA256为5a337427d120343f93445b95d783abd2bc0585eafe30aa7c5dedcaf690cddfff，与交付build-manifest和本地验证报告一致。run-config记录startupPolicy=bypass，startupIdleMsRequested/Effective=0，7500点、32bit、四卡。代码中0路径不进入startup缓存、空闲过滤或同步确认等待；原始轨迹也没有对应过滤事件。

只读审查产品，未修改任何产品代码、驱动、实机数据或分支。

## 日志完整性和边界

- paimage manifest列出的43个文件SHA256全部通过。
- 一个有完整配置的采样run：b4f6cdf2-4c87-42d1-8ac8-ddfd7f07b81b。原始轨迹5,952,405条，序号无缺失，traceIncomplete=false；成功recvfrom 2,681,034次，含8个反馈包；数据报文2,681,026，其中3次重复，2,681,023次SourceCore Accepted。
- 63,828个完整卡触发、12个不完整卡触发，共63,840个已观察卡触发。四卡各15,957完整＋3不完整；与前端最终累计值一致。
- timing轨迹2,532,200条，queueDropped=0、writerUnwritten=0、timingIncomplete=false。
- looplog不完整：签发3,388,156、写入3,355,443、未写32,713条。已保存序号1—3,355,443连续，时间覆盖23:55:16.339—00:04:10.966。第四轮最后约13.5秒缺少循环日志，但原始报文和timing仍完整。四轮首包前后各5秒窗口均在完整的已保存循环前缀内。
- 附件有两个应用run的事件上下文，不代表两套完整采样轨迹；另一应用run用于随后重启导出。没有将触发号1—4001这一未提供的范围推算为本次丢包。

## 四轮数量核实

每卡每触发应42包。以下按四卡均完整统计，不依赖首号是否为多余物理触发。

|开始时间|观察到的首末号|每卡实际收到触发数|四卡完整触发数（含首号）|不完整触发号|四卡应用入口全缺号|随后完整连续段|
|---|---|---:|---:|---|---|---|
|09-11 23:56:15|4002—8002|3997|3996|4002|4003—4006，4个|4007—8002，3996个|
|09-11 23:58:28|8003—12003|3965|3964|8003|8004—8039，36个|8040—12003，3964个|
|09-12 00:00:37|12004—16004|4001|4001|无|无|12004—16004，4001个|
|09-12 00:02:44|16005—20005|3997|3996|16005|16006—16009，4个|16010—20005，3996个|

三次不完整首号的四卡收包数分别27/27/27/28、16/16/19/18、16/16/18/19，应收42包。缺包分别59、99、99包，合计257包。整触发缺口44个，按四卡每触发168包折算7392个预期未见报文；不得与257个部分帧缺包混淆。

四轮首末号跨度都为4001，四卡一致且跨轮连续。若沿用此前确认的“每轮多余首触发＋4000有效触发”，完整有效数分别3996、3964、4000、3996，合计15956/16000，缺44个，全部集中启动段。缺失号位于内部，不受4000／4001总数的一位差影响。

用户最新说明外部总触发可能为4000或4001，因此以上“有效”归属仍需每轮确切物理条件确认。当前日志能证明协议序列缺口，不能独立测量物理触发次数，更不能证明采集卡未收到触发。不应简单把所有轮次套成4000，也不能仅从首末号跨度反推物理次数。

## 保存数据06—07

完整读取全部DAT并记录SHA256。7500点、float16，每通道每触发15000字节。

- 06：八通道每通道59,475,000字节＝3965条，对应23:58:28一轮。包括首号8003的不完整记录，后3964条完整；缺36个内部触发号。
- 07：八通道每通道59,955,000字节＝3997条，对应00:02:44一轮。包括首号16005的不完整记录，后3996条完整；缺4个内部触发号。
- 逐卡保存队列入队、消费和saveConsumer=true分别3965、3997；与DAT条数一致，没有发现额外保存条数损失。
- 06首记录各卡26/26/23/24个缺包对应区域、07首记录26/26/24/23个区域，A/B分别验证全部为零。无整条全零帧。
- 另两轮保存关闭，不能声称由DAT交叉验证。

这说明保存文件数量不足与应用入口未见的启动触发号相符；不是本次文件路径问题或保存队列丢弃的证据。原始trace没有payload，DAT无逐条触发头；验证基于保存顺序、长度和缺失区域一致性，不是UDP与DAT逐样本对拍。

## 启动时序与原因排查

### 原启动过滤不是本次原因

SourceCore在bypass中confirmed=true，现场无startup filter事件。数据入口记录先于组包和准入；缺失号根本没出现在成功recvfrom记录中，不能由其后的过滤、组包、保存或成像丢弃解释。

23:56:01.092执行START，23:56:02.118执行STOP，23:56:03.630再次START；第一轮首报文在23:56:15.330才到，间隔约11.7秒。后四轮共用同一启用状态，没有在各轮首包附近重新执行START／清理。没有Disabled、RecentTrigger、OffsetOutside、CompleteStart清理、源缓存清理或输出队列满事件；仅3个重复报文被去重。

### 未看到可解释大段缺失的应用长停顿

独立解析80字节looplog，并按每轮首包前后各5秒统计：

|首号|窗口最大完整循环间隔|最大单socket drain|卡1从首号到下一可见号|二者间循环数|
|---|---:|---:|---|---:|
|4002|3.0404ms|0.1367ms|4002→4007：1.7055ms|24|
|8003|3.4283ms|0.1582ms|8003→8040：18.2302ms|36|
|12004|3.1407ms|0.1525ms|12004→12005：99.7056ms|120|
|16005|3.6332ms|0.1678ms|16005→16010：7.1240ms|17|

窗口内没有Stalled提示；失败recvfrom仅记录10035（源码标记为would-block），未出现硬错误。各窗口之前5秒也有连续循环记录。

这不支持把缺4／36个触发直接解释为接收线程在首次来包附近连续停顿100／900ms。接收线程在首号与后续号之间仍运行。不能据此排除短时调度、系统排队或入口之前的问题：应用时间是从socket取出的时间，不是物理到包时间。

### 成像和保存没有对应丢弃证据

成像旁路峰值深度1，QueueFull/QueueBusy/CallbackFailed/processFailed/blocksSkipped均0；maxWorkerNs最大4,912,100，maxSubmitNs最大414,300。原始输出队列无CardFull／SyncEvicted／CallbackFailed。Disabled表示成像关闭，不是采集丢失。

前端统计是累计完整／不完整帧计数，并非当前物理轮次的完整性证明。最终每卡15957个完整触发＝3996＋3964＋4001＋3996，3个不完整触发均来自三个启动首号。持续显示不足或缺包累计值，不等于缺失还在持续发生；不能确定用户观察时具体看的是哪项计数。

## 已定位的诊断实现问题（不是已证明的采集丢包根因）

1. LoopLog.cpp实际持续写入整个监听期，没有实现任务书要求的滚动保留最近5秒并冻结启动窗口。长监听最终用尽256MiB，第四轮末段循环证据丢失。
2. 同文件预算耗尽标志存在边界错误：记录80字节，预算268435456字节，最后可写3355443条，共268435440字节，余16字节。下一条走bytes+80>budget分支，却置writeFailed_而不是budgetExhausted_；因此现场budgetExhausted=false不代表预算未满。summary又从局部failed输出writeFailed，造成writeFailed=false、writerUnwritten>0和loopLogIncomplete=true组合。能由代码和文件字节数对应确认。
3. 没有取得实际系统分层证据。附件和工作区查找均未发现本次ETL／PcapNG／成功capture manifest；发现的只有burst通知和交付时的权限失败验证文件。通知trialId为空、trialIdSource=command_line，不能作为抓取启动成功证据。无法判断用户是否在工作区之外抓取过。

第一、二项需修复诊断留存及标记，但不会恢复已缺的采集报文，也不足以解释启动缺失，因为四次启动窗口全部保留。

## 下一步与需要补齐的信息

现有信息已能排除本次原启动过滤和观察到的保存／成像输出丢弃路径，不能定位更底层的根因。遵循证据不足即停止的要求，不实施猜测性采集修复。

需要补齐两项：

- 如果本次运行过startup_ingress_capture.ps1，请提供其OutputDirectory，包括成功／失败manifest、命令输出和ETL／PcapNG；现有ZIP只有应用侧证据。若没有运行，下次只做30微秒启动段真实系统抓取，并先验证脚本启动成功，不需要扩展50微秒硬件实验。
- 对四个开始时间分别确认实际物理次数，以及是否包含多余首触发；若“持续缺失”指另一次采集或其他前端指标，需要对应轮次及指标名。内部4／36／4个号缺口不因首触发口径变化而消失，但有效总数必须有明确物理依据。

建议先修复循环日志留存／预算标记，再确保系统抓取正向验收，保持bypass及30微秒接收行为不变。未获得分层证据前，不设置启动延迟、不丢弃更多首段数据、不清socket、不调整网卡／线程参数。

## 可复核文件

本目录包含evidence、analysis-summary.txt、独立逐包统计independent-groups.json、rounds.json、loop-window-checks.json、saved-data-checks.json、hash-checks.json及复现脚本analyze_all.py、independent.py、reconcile.py。

