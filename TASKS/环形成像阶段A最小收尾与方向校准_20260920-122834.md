# 环形成像阶段 A 最小收尾与方向校准

发布时间：2026-09-20 12:28:34（发布机器本地时间）
目的：尽快结束阶段 A，只补一项缺失证据和必要的方向说明，不再扩展参考研究。

## 1. Objective

在已通过的参考工作上完成：
1. 实测起点/尾端报告区间的长参考稳定性，纠正此前把中心区间结果当作端点证据的回执。
2. 将反演明确定位为可关闭的工程近似，不宣称严格二维或双声速解析 FBP。
3. 撤回“反演必须换新权重”的强结论，明确阶段 B 的最小实现优先级。
4. 将 DBR 组合拒绝明确为本版保守实现限制，不作为数学禁忌或无失真保证。

阶段 A 完成标准是“参考算法与兼容规则足以指导实现”，不是“证明真实成像收益”或“所有边界误差归零”。真实数据质量、性能、UI和生产回归仍属于 B/C。本任务不自动解锁 B/C。

## 2. Baseline / exact starting SHA

继续实现分支：`codex/ring-zero-phase-pa-inversion-20260919-025754`
精确起点：`77b1d37e34dfbbf4b4f799328b8737a14f026339`（发布时已直接查询远端确认）。
原生产基线：`fb10721e07f9ea8c9e46bc308d250b788defb829`。

任务读取必须显式刷新引用：

```powershell
git fetch origin refs/heads/codex/task-docs:refs/remotes/origin/codex/task-docs
git show origin/codex/task-docs:TASKS/环形成像阶段A最小收尾与方向校准_20260920-122834.md
git fetch origin refs/heads/codex/ring-zero-phase-pa-inversion-20260919-025754:refs/remotes/origin/codex/ring-zero-phase-pa-inversion-20260919-025754
git switch codex/ring-zero-phase-pa-inversion-20260919-025754
git merge --ff-only origin/codex/ring-zero-phase-pa-inversion-20260919-025754
git rev-parse HEAD
git status --short
```

读取最新 main 的 PROJECT_STATUS.md、REPOSITORY_BASELINE.md、BUILD_STANDARD.md。起点变化或有独有工作先盘点，不覆盖，不为同步文档 merge/rebase main。

## 3. Target implementation branch

沿用上述分支，新增提交，不 amend/rebase/squash/force push、不合并 main、不在任务规格分支提交参考代码。

## 4. Problem / Evidence

此前 C1 核心判据、C2 配置矩阵、C3 查询策略已确认完成，规划侧已独立复跑 11/11 成功，无需再次重做。

剩余证据缺口：`CODEX_REPORTS/ring-zero-phase-pa-inversion-20260919/review-final-closure.md:57` 声称起点/尾端已通过不同长窗口确认，但实际收敛计算仍在目标 τ=800 的中心区间 [300,1300]，起点 burst 与尾端 τ=3600 敏感性只用一个最大长参考。人工判据正例不能替代这两个场景的实际信号比较；8000是参考pad，不是非参考pad。此次修正这一个明确缺口即可。

方向偏差：初版错误实验曾支持“只换信号而不换权重必然严重劣化”，该证据已作废。当前公平比较显示旧默认权重和立体角权重各有取舍。报告不得继续以错误因果支撑生产选择。

## 5. Invariants / 保留成果

保留现有长参考真实扩展区间、padConvergenceCheck、timeDerivative、R1/R2公平对照、C2/C3规则和对应已通过测试；不重写。

保留单链路架构、高低通独立开关、反演独立开关、双声速直线分区走时、MinDistance、apod默认none、距离指数退出UI的目标。

全关和仅HP/LP保持旧DAS查询；反演开启时未裁剪查询 q_raw=s+D−1，乘子 t=s/fs。不得借方向校准重开已解决的索引问题。

## 6. Prohibited Scope

不修改生产源码/UI/ABI，不构建DLL/运行包，不开始B/C；不新增体模大矩阵、文献综述、滤波器家族、严格二维反演、折射模型、用户参数；不重复exp1–exp6全量运行作为验收前提；不以追求零边界误差延长阶段A。

## 7. Necessary Work（仅两项）

### A. 小型端点稳定性补测

复用longReference和padConvergenceCheck，新增小脚本或扩展既有边界测试的局部段：

- 场景一：原起点burstOnly，采用此前实际报告区间。明确0基声学坐标与MATLAB索引；若原区间是索引1:1500，应记为τ=0..1499。
- 场景二：原近尾echoOnly，τ=3600，使用原敏感性指标实际计算区间；不能写超出短窗末端的未截断区间。记录实际取样上下界。
- 每个场景用至少两个不同非参考pad（优先2000、4000）及参考pad8000。对相同物理区间的p/p′/b计算相对参考误差及两窗差，使用现有1e-9判据；同时记录绝对误差与尺度。
- 若上述长度不足，仅延长参考/候选pad一次以形成稳定区间；不得放宽阈值或用自比计入通过。若仍不满足，报告未收敛和对原敏感性数值可信度的影响，不无限调参。
- 报告起点/尾端原敏感性数值在稳定参考下是否改变。这里检验的是长参考稳定性，不要求短窗与长窗误差小或为零。
- 将该小测试纳入统一入口；实际只需运行新增/受影响测试。修正review-final-closure中的错误证据归属和“8000是非参考pad”表述。

交付一份紧凑JSON和可复跑脚本即可，不重复导出exp2图像或其他无关MAT文件。

### B. 一页方向与阶段B入口说明

在algorithm-stage-A.md/README中就地修正，并提供简短closeout摘要，写清三个方向校准：

**反演定位**：当前候选b=2(p−t·p′)是复用现有DAS反投影框架的可关闭光声反演增强。单环应用和双声速直线走时属于工程近似；不声称严格二维FBP、非均匀介质精确反演或已证明实际图像全面改善。测试充分性仅限明确覆盖条件。

**权重选择依据与最小实现优先级**：旧默认固定q=1和立体角权重均有有效对照，前者并非被证明不可用于反演。明确撤回“只换信号一定严重劣化”“因此必须换新权重”的主张。作为规划侧的阶段B起草优先级，先评估保留旧固定q=1与accW、加入反演信号的最小候选（已有legacyW参考），立体角权重保留离线比较依据；不要求本轮再跑权重实验，不新增可调权重UI。该优先级不是在本任务中修改生产公式的授权，也不是把legacyW称为严格UBP：最终生产权重及归一化由阶段B任务明确，阶段A不再以证明某权重全面占优为退出条件。无需执行代理重复返回无限选项。

**DBR限制定位**：C2拒绝规则仍按已发布规格保留，文案定位为“本版未支持/未验证的组合”，不是数学上不能对含置零前缀信号做零相位滤波。通过配置校验不保证端点无失真；保留起点与近尾误差及实机UNVERIFIED。不得在此次文档收尾中擅自放开组合或扩大拒绝范围。

阶段B的最小目标仍是：生产高低通SOS、反演开关与必要局部适配、参数/UI/defaults、关闭路径回归和真实数据比较入口。双声速不扩展为新物理模型。阶段C评价真实质量及实时性能。阶段A不代替B/C结论。

## 8. Implementation Freedom

可选择把小测试放在新文件或现有边界测试中。选最少改动，复用现有模块，不重构公共数值代码。若公共模块发现真实缺陷，报告证据与影响，不顺手扩大范围。

## 9. Expected Changed Files

限 `CODEX_REPORTS/ring-zero-phase-pa-inversion-20260919/`：
- 必要端点稳定性小测试、统一入口一行注册；紧凑JSON。
- algorithm-stage-A.md、README.md、review-final-closure.md的相关段落。
- 一份简短阶段A收尾回执。

不要复制所有历史回执或大规模改写整篇报告。

## 10. Required Tests

仅必跑：新端点稳定性测试及任何因本轮修改而受影响的测试。原C1负例、C2/C3、R1/R2若代码未改，可引用77b1d37的已验证证据并明确本轮未重跑，不以省略这些重复运行判任务不完整。

新增测试失败必须非零退出。记录命令、工作目录、版本、退出码、候选/参考pad、实际区间、p/p′/b绝对与相对差、判据结果。不要把参考稳定与短长窗一致混为一谈。

## 11. Regression Baseline

77b1d37的已通过算法/配置/查询参考为保留基线。本轮不声称生产回归。原错误端点稳定性表述撤回，由新增实测证据取代。

## 12. Build / Test Commands

本次无生产构建要求。用MATLAB R2023a执行新增小测试，报告实际命令，例如：

```powershell
# 工作目录 CODEX_REPORTS/ring-zero-phase-pa-inversion-20260919/matlab
matlab -batch "run('<新增端点稳定性脚本>.m')"
```

文件名由执行代理确定并在回执给出，交付时不可保留占位命令。

## 13. Acceptance Criteria / 阶段A退出标准

- 起点与尾端稳定性有各自实际窗口的非自比证据，回执如实反映结果。
- 三个方向校准明确，撤回失效的必然优劣结论，DBR限制不被误写为数学禁忌。
- 已确认成果无回退，无新增生产改动、参数或研究范围。
- 报告明确：参考准备完成待最终审查；真实图像收益/性能/UI/生产实现未完成。

达到以上即可提交阶段A最终审查，不再主动追加新的研究项。本次不要求证明单环近似全面优于DAS。最终APPROVE只覆盖阶段A参考准备；B/C仍需后续明确任务。

## 14. Required Execution Report / 发布回执

简要返回任务名、分支、starting/final SHA、端点补测结果、三项文档修订位置、实际运行命令/退出码、引用但未重跑的旧证据、未验证项。不要再把已完成的R1/R2列为待整改。

```powershell
git push origin HEAD:refs/heads/codex/ring-zero-phase-pa-inversion-20260919-025754
git fetch origin refs/heads/codex/ring-zero-phase-pa-inversion-20260919-025754:refs/remotes/origin/codex/ring-zero-phase-pa-inversion-20260919-025754
git rev-parse HEAD
git rev-parse origin/codex/ring-zero-phase-pa-inversion-20260919-025754
git ls-remote origin refs/heads/codex/ring-zero-phase-pa-inversion-20260919-025754
```

三处SHA一致后返回，不合并main。

## 15. Hardware / system validation boundary

仅合成参考及执行规格准备。真实成像质量、运行节拍、硬件行为仍UNVERIFIED，不因阶段A结束而更改项目已有硬件结论。
