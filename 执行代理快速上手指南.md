# 执行代理快速上手指南

## 工作区与 Git

- 进入当前 worktree 后先执行 `git status`、`git branch --show-current`，确认没有误在 `main` 或 `codex/task-docs` 上改动。
- 任务文档：`git fetch origin`，再用 `git show origin/codex/task-docs:TASKS/<任务文件名>` 读取。
- 本环境优先使用 HTTPS + Git Credential Manager；不要为了解决推送问题改用 SSH 私钥。
- 若 `.git/worktrees/.../index.lock` 报 `Permission denied`，用允许的提升权限重试 `git add/commit`；不要删除未知的 lock 文件。
- 普通 GitHub `:443` 连接失败时，先判断是否为沙箱网络限制，再用允许的提升权限重试。
- 推送后比较本地与远端 SHA：
  `git rev-parse HEAD`
  `git ls-remote https://github.com/ZiXuannnZhang/PAErealtimeImaging.git refs/heads/<branch>`

## Qt/MinGW 测试 DLL

- CTest 出现 `0xc0000135`，优先判断为运行时 DLL 缺失，不要先改测试断言。
- 不要只依赖父进程 `PATH`；将 Qt/MinGW 运行库部署到每个 Qt 测试可执行文件旁边。
- 至少检查 `Qt6Core`、`Qt6Gui`、`Qt6Widgets`、`Qt6Concurrent`、`libgcc_s_seh-1.dll`、`libstdc++-6.dll`、`libwinpthread-1.dll`；Widgets 测试还要检查对应 `platforms/qoffscreen.dll` 或 `qwindows.dll`。
- 修复后重新配置、构建并运行完整 CTest；把 DLL 缺失、端口冲突和真实测试断言失败分开记录。

## UDP 8000–8004

- 测试前检查占用：
  `netstat -ano -p udp | Select-String ':8000|:8001|:8002|:8003|:8004'`
- 通过 PID 反查完整路径和命令行，确认是本任务允许处理的进程后，再停止精确 PID：`Stop-Process -Id <pid> -Force`。
- 停止后重新检查端口；不要静默修改生产端口或批量杀进程。
- 长期测试隔离应注入测试端口，同时保留正式端口占用/重绑契约测试。

## `cufft64_12.dll` 与成像运行时

- 不要把约 297 MB 的 `cufft64_12.dll` 提交到仓库。
- 使用外部运行时目录，并通过 `IMAGING_RUNTIME_DIR` 指定；目录必须同时包含 `pa_recon_core.dll` 和 `cufft64_12.dll`。
- 构建前可设置：`$env:PAIMAGE_IMAGING_RUNTIME_DIR='D:\path\to\runtime-bundle'`，或传 CMake 参数 `-DIMAGING_RUNTIME_DIR=<bundle>`。
- 迁移脚本先做预检：`迁移重建构建缓存.ps1 -ImagingRuntimeDir <bundle> -CheckOnly`。
- 构建前核对关键 DLL 的 SHA-256 和来源；构建产物旁边应能找到所需运行时 DLL。

## 网络权限与硬件验收

- `Get-NetAdapter`、`Get-NetIPConfiguration` 可能因权限返回 `Access denied`；可先用 `netstat`、`arp`、进程查询和应用日志完成软件侧判断，详细网卡/驱动检查再用提升权限 PowerShell。
- ARP 动态项、UDP 收包和 `phase=confirmed` 只能证明网络/软件侧可达，不等于 FPGA、网卡、配置和 CUDA 的硬件验收。
- 硬件验收由用户在真实硬件、目标网卡和正确 FPGA 配置下完成；代理应明确标记为 `not_verified`，除非收到完整现场证据。
- 若必须进行桌面网络管理且电脑处于锁屏、无法解锁，应报告该阻塞并跳过这部分，不要假设操作已完成。

## 交付前检查

- `git diff --check` 通过。
- 配置、构建和完整 CTest 已执行；精确记录失败测试及其类别。
- 复核端口占用、进程路径、运行时 DLL 存在性和关键 SHA-256。
- `git status --short` 清洁；提交并推送后再次核对本地/远端 SHA。
- 交付说明中单独写明软件验证结果、未解决测试契约问题和硬件验收状态。

通用的双端 GitHub 联动流程见：`Codex-GitHub双端联动快速上手.md`。
