# 前端全分辨率显示基础清理：构建与回归验证

发布时间：2026-09-22 01:30（UTC+8）
任务类型：VALIDATION-ONLY

## 1. Task

对以下实现提交执行标准 Windows 构建和软件回归验证：

~~~text
branch: codex/frontend-display-fullres-20260922
SHA:    c31fd09aa7e1c0707faf121448be49bad9d6dfce
~~~

本任务只做验证，不修改实现。

## 2. Start state

先读取：

~~~powershell
git fetch --prune origin
git show origin/main:BUILD_STANDARD.md
git show origin/codex/task-docs:TASKS/前端全分辨率显示基础清理构建与回归验证_20260922-013000.md
~~~

切换到目标分支：

~~~powershell
git switch codex/frontend-display-fullres-20260922
git merge --ff-only origin/codex/frontend-display-fullres-20260922
git rev-parse HEAD
git status --porcelain --untracked-files=no
~~~

开始构建前必须满足：

~~~text
HEAD = c31fd09aa7e1c0707faf121448be49bad9d6dfce
tracked working tree = clean
~~~

若不满足，停止并报告，不进行 reset/rebase/force/amend。

## 3. Scope restrictions

本任务禁止：

- 修改 src/include/ui/tests/CMake/构建脚本；
- 修改任何 production/test/build input；
- merge/rebase/cherry-pick main 或其他分支；
- 修复构建或测试失败；
- 新增任何功能；
- force push。

若发现失败，只保存证据并报告。

## 4. Build

按 BUILD_STANDARD 使用标准 MinGW Debug 构建：

~~~powershell
Set-Location "D:\ChatGPT\PAERealtimeImaging\MC_410T_MultiCard\delivery"
cmd /c build_mingw_debug.cmd
~~~

记录：build command、exit code、toolchain versions、native bin path、BuildIdentity/source SHA、tracked clean state。

至少确认以下文件存在：

~~~text
build/mingw_debug/bin/PAimageReceiverDiagnostics.exe
build/mingw_debug/bin/ImagingSvc.exe
build/mingw_debug/bin/ring_svc_selftest.exe
build/mingw_debug/bin/ring_udp_replay.exe
~~~

BuildIdentity 必须对应 c31fd09aa7e1c0707faf121448be49bad9d6dfce。

## 5. Targeted regression

在本次新构建的 test tree 上执行：

~~~powershell
Set-Location "D:\ChatGPT\PAERealtimeImaging\MC_410T_MultiCard\delivery\build\mingw_debug"
ctest --output-on-failure -j 1 -R "^(paimage_host_output_test|data_processor_batch_test|data_processor_imaging_isolation_test|session_boundary_test|session_boundary_receiver_test|filesaver_round_boundary_test)$"
~~~

逐项报告 PASS / FAIL / SKIP / NOT_RUN：

~~~text
paimage_host_output_test
data_processor_batch_test
data_processor_imaging_isolation_test
session_boundary_test
session_boundary_receiver_test
filesaver_round_boundary_test
~~~

## 6. Full regression

执行：

~~~powershell
ctest -N
ctest --output-on-failure -j 4
~~~

记录 ctest -N total、PASS、FAIL、SKIP/NOT_RUN 和失败测试名。

如 full CTest 出现失败，可对失败项单线程重跑一次用于诊断，但必须保留原始 full CTest 结果，不得无限重跑。

## 7. Evidence receipt

验证完成后，允许在同一实现分支追加一个仅包含文本证据的 receipt commit。

目录：

~~~text
CODEX_REPORTS/frontend-display-fullres-20260922/validation-c31fd09a/
~~~

至少提交：

~~~text
validation-receipt.md
build-summary.txt
ctest-targeted.txt
ctest-full.txt
git-receipt.txt
~~~

receipt commit 不得包含任何 source/test/build-input 修改。

记录：

~~~text
BUILD_SOURCE_SHA = c31fd09aa7e1c0707faf121448be49bad9d6dfce
REPORT_HEAD      = <receipt commit SHA>
~~~

检查：

~~~powershell
git diff --name-only c31fd09aa7e1c0707faf121448be49bad9d6dfce <REPORT_HEAD>
~~~

差异只能位于 CODEX_REPORTS/frontend-display-fullres-20260922/validation-c31fd09a/。

随后：

~~~powershell
git push origin HEAD:refs/heads/codex/frontend-display-fullres-20260922
git fetch origin refs/heads/codex/frontend-display-fullres-20260922:refs/remotes/origin/codex/frontend-display-fullres-20260922
git rev-parse HEAD
git rev-parse origin/codex/frontend-display-fullres-20260922
git ls-remote origin refs/heads/codex/frontend-display-fullres-20260922
~~~

三处最终 SHA 必须一致。

## 8. Completion criteria

最终只报告事实结果：

~~~text
Build source SHA
Report HEAD SHA
Build result
BuildIdentity
Four core exe existence
Six targeted test results
Full CTest total / PASS / FAIL / SKIP / NOT_RUN
Evidence directory
Receipt-only diff result
Local HEAD
Remote HEAD
ls-remote HEAD
~~~

如果 build 或 required regression 失败，不修源码，直接提交失败证据并报告。