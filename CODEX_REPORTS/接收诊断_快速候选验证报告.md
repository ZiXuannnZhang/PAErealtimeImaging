# 快速候选验证报告

2026-09-10。用户最新要求优先快速迭代，单次采集不超过两分钟；取消余下长压力测试。此交付为应用诊断候选，未完成原任务书的系统分层定位全部验收。

## 构建和隔离

分支 codex/receiver-diagnostics-20260910，基于任务书提交 df84842bc51cd5ba4fad63d33aaff347d544a532 的未提交修改。GNU 13.1.0、Qt 6.8.0、CMake Debug。独立 EXE、窗口标题和 INI。原包、原数据、原分支不改；未运行真实硬件。构建目录 receiver-diagnostics-debug，交付 artifacts/ReceiverDiagnostics。build-manifest.json 记录实际 EXE/文件哈希及修改源码哈希。

## 已验证

- C++ 回归共 25 项。并行一轮中 data_processor_batch_test 因 receiver start 失败，单独复测通过；串行一轮中 paimage_network_test 的保存文件大小断言偶发失败，随后单独连续复测两次均通过。因此测试集存在时序不稳定项，未将其写成稳定全绿。活动/停止导出固定切面和失败重试测试通过。
- 独立分析 fixture 覆盖缺号、文件乱序、短尾、schema 兼容、重用触发、跨 session 精确关联、轮次歧义及回绕，全部通过。
- 真实附件：3,706,288 条，无缺号；47,028 卡触发，46,904 完整、124 部分；各卡内部缺槽 563/568/562/564。三轮有效观测 3917/3924/3911，部分 2/6/19，完全未见 83/76/89。详见 receiver-real-regression.json。保存原输出 3912 与有效观测 3911 按任务书区分，没有改写原数据。
- 最终生产 NetworkController 短回放：35 包/100 Hz/10 秒，4000 宿主卡回调，sender 最大迟到 25.8574 ms；原轨迹对照 35 包/100 Hz/3 秒，1200 回调；70 包/40 Hz/3 秒，480 回调。均 PASS，样本值校验通过，保存关闭。对应轨迹 313225/94125/71265 条，均完整；时序模式 214919/53361 条，均完整。
- 保存开启和 20→50→20 微秒配置切换由常规生产链路测试覆盖，文件大小、连续编号及数据通道检查通过。
- WPR 自定义调度 profile 在本机解析通过，列出 CSwitch、ReadyThread、DPC、Interrupt。普通权限 Pktmon 报拒绝访问；未将其报告为系统抓取通过。

## 诊断预算与范围

入口内存 16 MiB、磁盘 1 GiB，保持原格式；时序内存 16 MiB、磁盘 256 MiB。最终 100 Hz 短测时序约 2.15 万条/秒，按两分钟估算约 157 MiB；入口按该短测估算约 229 MiB。预算是估算，记录达到上限或异常丢失仍报告 incomplete。每次监听运行不超过两分钟，包括空闲；下一实验重新开始监听。

用户调整范围前已完成一次旧组件版十分钟回放：840 万报文全部收到，但入口达到 1 GiB 上限，不能当作成功验收。该发现促成预算核对；后续压缩尝试已撤回，没有进入交付。正在运行的另一长测已取消，不计入通过结果。

## 明确待完成

系统抓取启动/停止在快速候选禁用，避免未验证的全局会话清理。系统 ETL 关联、drop reason、调度状态、时钟映射误差、阶段开始快照、全套延迟注入、完整轮次保存结算和多轮 1+4000 仿真尚未完成。活动时序导出可能标记 incomplete，建议停止监听后导出。GUI 由用户操作验收。

本轮结果只能支持应用入口和阶段耗时分析，不能确认网卡/驱动根因或四卡同因。根据下一轮实机数据决定后续增强。
