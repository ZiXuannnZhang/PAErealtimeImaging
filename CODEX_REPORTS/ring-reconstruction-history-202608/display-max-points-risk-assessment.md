# 显示最大点数改动风险评估报告

> 目标：数据始终以全分辨率实时显示（显示点数 = 全采样点数，取消显示降采样）。
> 结论先行：**可实施，但视觉收益低、性能风险中低，建议保留现有控件并仅调整默认值；若坚持全分辨率显示，建议分阶段实施并在平铺 8 通道场景实测后定案。**

## 一、现状

| 项目 | 说明 |
| --- | --- |
| 控件 | `显示最大点数`（spnDownsampleRatio，默认 1000，范围 1000~500000） |
| 数据流 | `cfg.displayPoints` → `DataProcessor::downsample()` 生成 `freqA_display/phaseA_display`（显示快照，float→double 转换由 `DisplayBuffer::update` 完成） |
| 用途 | 时域/相位波形、频域幅值谱（FFT）、峰峰值统计——**仅显示层** |
| 与成像/保存的关系 | 无关：环形成像馈送与线性成像均使用**全分辨率** `freqA/freqB`（`updateFullRes`/`ringFeedSink`）；FileSaver 保存全分辨率 `freqA/freqB`。两者数据一致（已核查） |

## 二、改动方案（若实施"始终全分辨率"）

- 方案 A：将 `displayPoints` 固定为采集深度（真实采集 4000，线性测试 12500）；
- 方案 B：删除控件并在代码中硬编码全分辨率。

## 三、风险点与影响面

1. **显示数据量放大 4~12.5 倍**（4000/12500 vs 1000）：
   - `DisplayBuffer::update`：每卡每触发 4 向量 float→double 转换与拷贝，写线程 CPU 随点数线性增加（100 Hz×4 卡时最明显）；
   - UI 线程：快照拷贝、`setData`、峰峰值扫描、FFT（n=4096/16384）随点数线性增加；
   - 平铺 8 通道 × 30fps 的 UI 计算量同步放大（渲染本身已优化：自适应采样 + 关闭抗锯齿 + 1px 画笔，绘制成本可控）。
2. **频域幅值谱**：全分辨率 FFT 单次 16384 点约 0.2ms，可接受；但若未来平铺模式也显示频域则 ×8。
3. **内存**：显示缓冲每卡 4 向量 × depth × 8B ≈ 400KB（4000 点），8 卡约 3MB，可忽略。
4. **视觉收益低**：图窗宽约 600~900px，1000 点已超屏幕横向分辨率；波形绘制经自适应采样按像素取点，全分辨率在默认视图下无可见差异。真正需要全分辨率的场景是"放大查看细节"，现有控件调大即可满足。
5. **兼容与回归**：交付包（`PALiveImaging/delivery`）同步修改；回放数据（14.dat depth=4000）与真实采集（250MHz depth=4000）点数量级一致；需回归平铺 8 通道 + 实时成像同时开启的 UI 占用。

## 四、风险评估结论

| 维度 | 评估 |
| --- | --- |
| 性能风险 | 中低（数据路径均为线性放大；渲染层已优化；需实测平铺 8 通道 + 成像同时开启场景） |
| 视觉收益 | 低（默认视图 1000 点已覆盖屏幕分辨率） |
| 兼容风险 | 低（不影响成像与保存的数据一致性） |
| 建议 | **保留控件**；如需更高显示分辨率，将默认值由 1000 提高至与采集深度一致即可（用户仍可下调以换取性能）。不建议删除该控件。 |

## 五、当前状态

- 本报告仅评估，**功能未做改动**。
