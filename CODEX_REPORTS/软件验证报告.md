# 软件验证报告

2026-09-10，production-3交付版。生产采集替换、独立Debug构建与软件验证完成。用户已接管剩余Computer Use界面验收；实机效果未验证。

## 结果

| 测试 | 结果与证据 |
|---|---|
| 真实Windows UDP，4卡40Hz，28包，600秒 | 2,688,004入口报文（4 ACK），96,000完整卡触发，0部分；显示/Ring各96,000返回；traceIncomplete=false、associationComplete=true |
| 真实Windows UDP，4卡40Hz，70包，600秒 | 6,720,004入口报文（4 ACK），96,000完整卡触发，0部分；显示/Ring各96,000返回；traceIncomplete=false、associationComplete=true |
| 20→50→20 | 各22同步触发，共264卡触发；保存000/001/002保留且样本对应尺寸正确 |
| 生命周期 | 新生产100次Start/Stop，Start前及Stop后尾包观察到并拒于源接纳，不进入Ring；未配置Start失败后仍可CONFIG/START |
| 宿主值与格式 | 4卡5000/12500样本，B/A已知值，逐样本float16文件验证；没有旧处理/保存QThread |
| 保存积压跨代 | 阻塞第1代保存消费，排入第1/2代后释放；第1代2触发、第2代1触发各在原目录，未错路由 |
| 源边界组件 | 重复/乱序/首中尾缺槽/回绕/recent窗口/启动确认与缓存限制/超时/Stop截断；卡FIFO满拒新、同步启动优先和整块淘汰 |
| 诊断自损 | 小队列饱和、磁盘预算耗尽、文件路径写错、分段轮转；不完整可见；分析器无虚假完整结论 |
| 导出 | 活跃writer固定序号、点击后旧timestamp记录排除、跨run、停止后、失败重试通过 |
| 离线分析 | 独立构造fixture检查短包、短尾、复用序号、跨session精确关联、缺summary；缺记录范围与跳号估计另列 |
| 受影响回归 | 24项非交互回归通过；diagnostic_dialog_test补齐qoffscreen.dll后3.29秒通过，合计25项分组通过；并非同一次全套运行 |

长测保存关闭、Publisher关闭，显示/Ring入口开启；不是2通道GPU整机负载验证。首轮28包maxSenderLateMs=16.1086，70包13.7758；使用绝对25ms时刻发送，所有计划数据报已发出。最大发送迟到不是采集端延迟。

长测在开发中间版本执行：28包早于导出fence/身份/保存代后续补充，70包含导出fence但早于保存代标记；后续相关代码经短测，不能把它们声称为最终提交逐字节的600秒复测。

## 生产已知故障

run 303c1c23-76bc-46c9-9bf5-81a8952ed864：2秒、80触发/卡、5000样本；仅省略卡索引0、trigger30、packet10。实际入口8,963（8,959数据+4 ACK）；319完整卡触发、1部分；源相对缺槽[10]，Ring/显示316。traceIncomplete=false，关联完整。缺包没有被启动过滤或补零隐藏。保存关闭会有CardDisabled，不将其记成保存丢失。

## 日志扰动短对照

同进程包含回放发送器和接收器，四卡40Hz/70包/10秒；非隔离CPU基准，只用于检查已知回放额外缺口。

| 项目 | trace关闭 | trace开启 |
|---|---:|---:|
| wallMs | 10018.2 | 10033.6 |
| CPU进程累计ms | 640.625 | 1578.12 |
| Ring返回 | 1600 | 1600 |
| maxSenderLateMs | 12.0935 | 13.1895 |

开启轨迹：112,004入口，1600完整、0部分，0缺记录。关闭无法独立复核raw，但计划sendto与Ring计数一致。CPU差额不能单独归因于接收线程，未证明零扰动。完整队列峰值见各run trace-summary.json。

## 证据与复现

build/production-28-600s.log、production-70-600s.log、production-compare-off.log、production-compare-on.log、production-missing-middle.log。长测run分别为b49e0acf-cf46-4803-9aa5-a8dd74a11a01、490b72f9-b92f-48e5-95c8-44f253da9625；在build/paimage-host-checks/paimage-traces下，其analysis.json及trace-summary.json为可复核证据。正常短对照run为636e29a6-2b24-4649-85e4-bdb50d7fd941。

执行build/paimage-host-checks/paimage_network_test.exe（无参数）验证短生命周期；参数600 5000或600 12500做长测；10 12500 off/on做诊断对照；2 5000 missing做故障输入。各测试写当前工作目录及EXE旁paimage-traces，请在本隔离worktree运行。

旧network_diagnostics_test以--legacy-control-only只检查历史控制辅助契约，不用旧StartFence证明新路径。没有原EXE动态对拍，没有外接网卡A/B，不报告物理首沿零缺包。


## 交付窗口验证

交付EXE标题、实验标记和空闲异步导出已实际操作通过。导出ZIP CRC及PAimage子清单文件SHA256通过，事件中trialId=gui-validation、roundId=1、dataTimeNs=50000。未启动采集，不将空闲导出算作采集中GUI验收；活跃writer导出由自动测试覆盖。部分旧runtime中文文本存在编码乱码，结构化标记字段可读；该旧文本问题不作为采集完整性依据。用户已要求停止Computer Use，参数修改/重启持久化和旧注册表前后相等未验收。证据见paimage-port-evidence/gui-offline-validation.json、diagnostic-dialog-passed.txt。
